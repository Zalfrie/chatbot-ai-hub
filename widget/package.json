{
  "name": "chatbot-hub-widget",
  "version": "1.0.0",
  "description": "Vanilla TypeScript embed widget for AI Chatbot Hub",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.js",
    "build:watch": "node build.js --watch",
    "dev": "node build.js --watch"
  },
  "devDependencies": {
    "esbuild": "^0.25.0",
    "typescript": "^5.9.3"
  }
}
